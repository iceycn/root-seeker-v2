# Context Assembler 三级目标索引

父级文件：`04-context-assembler.md`

## 三级文件列表

- `01-按优先级排序证据.md`：按优先级排序证据（可以暂时停止在三级）
- `02-控制字符和-token.md`：控制字符和 token（建议继续拆到四级）
- `03-记录-dropped-evidence.md`：记录 dropped evidence（可以暂时停止在三级）
- `04-evidence-context_assembler.py.md`：单独定义 ``evidence/context_assembler.py`` 的职责、输入输出和验收标准（建议继续拆到四级）
- `05-policies-budget.py.md`：单独定义 ``policies/budget.py`` 的职责、输入输出和验收标准（建议继续拆到四级）

## 使用方式

后续实现时，优先一次只选择一个三级文件；若该三级文件标记为“建议继续拆到四级”，先按其中的四级建议再拆一层。
