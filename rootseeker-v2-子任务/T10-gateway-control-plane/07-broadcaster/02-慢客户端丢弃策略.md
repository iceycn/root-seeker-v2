# 慢客户端丢弃策略

## 上级目标

- 父级任务：`T10-gateway-control-plane`
- 父级小目标：`07-broadcaster.md`
- 父级标题：Broadcaster

## 三级目标

慢客户端丢弃策略

## 输入

events

## 输出

client frames

## 建议落地文件

- `gateway/broadcaster.py`

## 四级细化判断

结论：可以暂时停止在三级。

原因：该三级目标已经接近一个类、一个函数入口或一个协议对象的粒度，可以在实现前再按实际代码复杂度决定是否继续拆。

## 停止继续拆分的条件

- 只对应一个类、一个函数入口、一个 DTO 或一个 schema 片段。
- 实现时预计只改 1 个文件，且测试点不超过 3 个。
- 输入、输出和失败模式已经可以被一句话说明。

## 验收标准

- 能直接映射父级小目标的验收标准。
- 能明确目标文件、输入对象、输出对象和至少一个测试点。
