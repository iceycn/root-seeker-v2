---
name: Catalog resolve service
description: "将告警服务名映射到服务目录条目。"
---

# Catalog resolve service

调用 `catalog.resolve_service`，使用归一化后的 `tenant`、`environment`、`service_name`。

详细准则见 `references/guide.md`。
