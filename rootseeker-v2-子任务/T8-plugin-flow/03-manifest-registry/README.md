# Manifest Registry 三级目标索引

父级文件：`03-manifest-registry.md`

## 三级文件列表

- `01-解析-manifest.md`：解析 manifest（建议继续拆到四级）
- `02-按优先级去重.md`：按优先级去重（可以暂时停止在三级）
- `03-合并-config-schema.md`：合并 config schema（可以暂时停止在三级）

## 使用方式

后续实现时，优先一次只选择一个三级文件；若该三级文件标记为“建议继续拆到四级”，先按其中的四级建议再拆一层。
