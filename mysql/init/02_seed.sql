-- Optional seed placeholders. Builtin cron jobs are seeded by AdminConfigStore on first load.
-- Keep this file for custom environments that want SQL-level seeds.

SELECT 1 AS rootseeker_mysql_init_ok;
